import { GameWrapper } from '../../components/game';

export const metadata = {
  title: 'Life@Dev v2.0 - Command Center',
  description: 'Strategic developer life simulation - Balance code, career, and sanity',
};

export default function GamePage() {
  return <GameWrapper />;
}
